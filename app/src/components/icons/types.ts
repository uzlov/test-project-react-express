/**
 * Shared types for icon components
 */

export interface IconProps {
  className?: string;
  size?: number | string;
  strokeWidth?: number;
}

export interface IconPropsWithFill {
  className?: string;
  size?: number | string;
}
